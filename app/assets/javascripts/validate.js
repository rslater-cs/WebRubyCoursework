function validate(){
    const email_field = $('#email');
    const password_field = $('#password');

    if($("#input_button").text()[0] === "R"){
        const reenter_field = $('#password_reenter');


    }

    alert(email_field.val() + " " + password_field.val());
}